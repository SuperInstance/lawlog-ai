{
  "name": "lawlog-ai",
  "version": "1.0.0",
  "description": "Contract companion that remembers every obligation across every agreement — your irreplaceable legal memory.",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "build": "wrangler deploy --dry-run"
  },
  "dependencies": {},
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240529.0",
    "typescript": "^5.5.0",
    "wrangler": "^3.60.0"
  }
}
